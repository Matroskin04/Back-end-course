export type blog = {
    id: string
    name: string
    description: string
    websiteUrl: string
}
export type bodyBlogType = {
    name: string
    description: string
    websiteUrl: string
}
export type checkErrorsType = {
    id?: string
    name?: string
    description?: string
    websiteUrl?: string
}
